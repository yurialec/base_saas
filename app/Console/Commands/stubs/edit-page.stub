<template>
    <section class="page">
        <Loading :loading="isLoading" fullscreen />

        <div class="d-flex flex-column flex-lg-row align-items-lg-center justify-content-between gap-3 mb-4">
            <h1 class="h4 mb-0">Editar {{ label }}</h1>
            <router-link class="btn btn-outline-secondary btn-sm d-inline-flex align-items-center gap-2"
                :to="{ name: '{{ route }}' }">
                <i class="bi bi-arrow-left"></i>
                <span>Voltar</span>
            </router-link>
        </div>

        <form class="row g-4 justify-content-center" @submit.prevent="update">
            <div class="col-12 col-xl-8">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-header bg-white border-0 pb-0">
                        <h2 class="h6 mb-0">Dados de {{ label }}</h2>
                    </div>
                    <div class="card-body bg-white">
                        <div class="row g-3 mb-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label">Nome</label>
                                <input v-model="{{ variable }}.name" class="form-control" type="text" required>
                            </div>
                        </div>
                        <div class="d-flex justify-content-end">
                            <button class="btn btn-primary btn-sm text-white" type="submit" :disabled="isLoading">
                                <span v-if="isLoading" class="spinner-border spinner-border-sm" role="status"></span>
                                <span v-else>Salvar</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </section>
</template>

<script>
export default {
    name: 'Edit{{ class }}',
    props: {
        id: {
            type: [String, Number],
            required: true
        }
    },
    data() {
        return {
            isLoading: false,
            {{ variable }}: {
                name: ''
            }
        };
    },
    mounted() {
        this.find();
    },
    methods: {
        find() {
            this.isLoading = true;

            axios.get(`/api/{{ route }}/find/${this.id}`)
                .then(response => {
                    this.{{ variable }} = response.data;
                })
                .catch(error => {
                    alertDanger(error);
                })
                .finally(() => {
                    this.isLoading = false;
                });
        },
        update() {
            this.isLoading = true;

            axios.put(`/api/{{ route }}/update/${this.id}`, this.{{ variable }})
                .then(response => {
                    this.$router.push({ name: '{{ route }}' });
                })
                .catch(error => {
                    alertDanger(error);
                })
                .finally(() => {
                    this.isLoading = false;
                });
        }
    }
};
</script>

<style scoped>
.page .card {
    border-radius: 0.5rem;
}
</style>
